// I personally prefer using .jsx rather than .js to distinguish components from pure logic files.
// You can use .js for everything if you prefer, though. React & JS compilers treat them the same.
export default function Hello() {
    return <p>This is my first component!</p>
}